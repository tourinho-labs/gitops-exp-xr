apiVersion: platform.lab/v1alpha1
kind: AppNamespace
metadata:
  name: exp-xr
  namespace: default
spec:
  teamName: exp-xr
  # Mesmo profile que dirige os resources do container (size-<profile> no
  # kustomization) → aqui vira a ResourceQuota do namespace.
  size: small
