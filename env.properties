# Variáveis de ambiente não-sensíveis da app (CHAVE=valor, uma por linha).
# Editado via Backstage (Edit Service) — vira ConfigMap app-env no deploy.
# Segredos NUNCA aqui: capabilities entregam Secrets (db-conn, cache-conn).
